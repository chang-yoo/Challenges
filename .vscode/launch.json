{
  "configurations": [
    {
      "name": "Run with Node.js",
      "program": "${file}",
      "request": "launch",
      "skipFiles": [
        "<node_internals>/**"
      ],
      "type": "node",
      "outputCapture": "std", // or "console" 
    }
  ]
}
